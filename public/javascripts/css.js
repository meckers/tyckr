// TODO: Find a way to dynamically load CSS in much the same way as we load JS.
// in the meantime, all (static) CSS will be defined here.

var CSS = {};

CSS.Bubble = {
    'background-color': 'white',
    'border': '1px solid black',
    'border-radius': '10px',
    'padding': '10px',
    'position': 'absolute'
};

CSS.BubbleTextInput = {
    'width': '200px',
    'height': '20px',
    'border': '1px solid black'
};